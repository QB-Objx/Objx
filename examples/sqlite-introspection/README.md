# SQLite Introspection Example

This folder shows the current `codegen` flow with concrete artifacts:

1. a database schema snapshot
2. the generated OBJX model

Use it as a reference for the current output shape before wiring the CLI into another project.
